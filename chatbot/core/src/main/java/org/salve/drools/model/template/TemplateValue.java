package org.salve.drools.model.template;

/**
 * Strings that are intended to be template values.
 * These are eventually used by the template engine to insert data
 */
public class TemplateValue extends ATemplate{
	public TemplateValue(String name) {
		super(name);
	}
}
